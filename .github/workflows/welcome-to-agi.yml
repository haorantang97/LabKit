name: Welcome to AGI
on:
  push:
    paths:
      - 'skills/welcome-to-agi/**'
      - '.github/workflows/welcome-to-agi.yml'
  pull_request:
    paths:
      - 'skills/welcome-to-agi/**'
      - '.github/workflows/welcome-to-agi.yml'
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python: ['3.10', '3.12']
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
      - run: python -m unittest discover -s skills/welcome-to-agi/tests -v
